name: "Reusable workflow to build and publish webui py package"

on:
  workflow_call:

jobs:
  foo:
    name: Build and publish web ui Python package
    runs-on: ubuntu-20.04

    steps:
      - name: "Determine run-environment"
        shell: bash
        run: |
          echo "Hello world!"

      - uses: actions/checkout@v3
        with:
          persist-credentials: false
          submodules: false

      - name: "Compile static web UI"
        shell: bash
        run: |
          ls -la .
